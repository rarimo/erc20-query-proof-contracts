import { ethers } from "ethers";

export const REWARD_AMOUNT = ethers.parseEther("10");
export const REGISTRATION_SMT_ADDRESS = ethers.ZeroAddress;

export const TOKEN_NAME = "Claimable Token";
export const TOKEN_SYMBOL = "CTK";
